name = "paw-patrol-ai"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[vars]
# 环境变量会在 Cloudflare Dashboard 中配置

# 开发服务器配置
[dev]
port = 8787
local_protocol = "http"
